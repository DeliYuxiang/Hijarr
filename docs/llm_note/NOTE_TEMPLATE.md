# 工作日志模板

**复制本文件到**：`docs/llm_note/note_YYYYMMDD_<agent>.md`

替换所有 `{{占位符}}` 后保存。

---

# 工作日志 — {{YYYY-MM-DD}} — {{Agent 名称}}

## 会话概览

| 字段 | 内容 |
|:---|:---|
| 日期 | {{YYYY-MM-DD}} |
| Agent | {{Claude Sonnet 4.x / GPT-4o / 等}} |
| 会话目标 | {{一句话描述本次目标}} |
| 完成情况 | {{已完成 / 部分完成 / 被阻塞}} |

---

## 必读清单确认

开始前确认已阅读：

- [ ] `CLAUDE.md` — 构建命令、架构、Agent 规范
- [ ] `README.md` — 功能概述
- [ ] `docs/progress.md` — 当前状态和活跃风险
- [ ] `docs/llm_note/` — 历次工作日志（按时间顺序）

会话开始时构建验证：
```bash
CGO_ENABLED=0 go build ./...
# 结果：{{OK / 报错信息}}
```

---

## 任务描述

{{详细描述任务。用户要求什么？预期结果是什么？}}

---

## 调查过程

### 已读文件

列出为理解当前状态而读取的每个文件：

- `{{文件路径}}` — {{读取原因 / 发现内容}}

### 发现

{{发现了什么？当前行为是什么？Bug 的根本原因是什么？}}

---

## 已做改动

### {{改动 1：简短描述}}

**文件**：`{{路径/文件.go}}`

**原因**：{{改动理由}}

**内容**：{{具体改了什么——详细到可以 review}}

```go
// 改动前（如适用）：
// {{旧代码片段}}

// 改动后：
// {{新代码片段}}
```

### {{改动 2：简短描述}}

*（按需重复）*

---

## 测试

```bash
CGO_ENABLED=0 go test ./...
# 结果：{{输出}}
```

额外运行的测试：
- `{{测试命令}}` → `{{结果}}`

功能验证（如适用）：
- {{在运行系统中验证了什么}}

---

## 遇到的问题

### {{问题 1}}

**现象**：{{出了什么问题}}

**根本原因**：{{为什么会发生}}

**解决方案**：{{如何修复 / 临时方案}}

---

## 对 progress.md 的影响

勾选所有适用项并说明变化：

- [ ] 完成了某功能 → 在进度.md 中标记 `[x]`
- [ ] 发现新风险 → 添加到活跃风险章节
- [ ] 解决了活跃风险 → 删除/更新对应条目
- [ ] 添加了规划功能 → 添加到路线图
- [ ] 更新了 Agent 工作日志索引

---

## 给下一位 Agent 的建议

{{下一位 Agent 需要知道什么？最重要的下一步是什么？
尽量具体：引用文件名和行号。}}

1. {{建议 1}}
2. {{建议 2}}

---

## 经验总结

{{什么出乎意料？什么不够直观？如果重来一次会怎么做？}}
